// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusSerialize, DofusType};
use dofus_io::boolean_byte_wrapper;
use super::*;
use anyhow::Result;

/// Protocol type — ID: 1204
#[derive(Debug, Clone, Default)]
pub struct GameRolePlayMerchantInformations {
    pub contextual_id: f64,
    pub disposition: Box<EntityDispositionInformationsVariant>,
    pub look: EntityLook,
    pub name: String,
    pub sell_type: u8,
    pub options: Vec<Vec<u8>>,
}

impl DofusSerialize for GameRolePlayMerchantInformations {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_double(self.contextual_id);
        writer.write_ushort(self.disposition.get_type_id());
        (*self.disposition).serialize(writer);
        self.look.serialize(writer);
        writer.write_utf(&self.name);
        writer.write_byte(self.sell_type);
        writer.write_short(self.options.len() as _);
        // polymorphic vector (unresolved base type)
    }
}

impl DofusDeserialize for GameRolePlayMerchantInformations {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            contextual_id: reader.read_double()?,
            disposition: {
                let type_id = reader.read_ushort()?;
                Box::new(EntityDispositionInformationsVariant::deserialize_with_id(type_id, reader)?)
            },
            look: EntityLook::deserialize(reader)?,
            name: reader.read_utf()?,
            sell_type: reader.read_byte()?,
            options: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(Default::default());
                }
                v
            },
        })
    }
}

impl DofusType for GameRolePlayMerchantInformations {
    const TYPE_ID: u16 = 1204;
}

