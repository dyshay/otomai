// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusMessage, DofusSerialize};
use dofus_io::boolean_byte_wrapper;
use super::super::types::*;
use anyhow::Result;

/// Protocol message — ID: 6296
#[derive(Debug, Clone, Default)]
pub struct DungeonKeyRingUpdateMessage {
    pub dungeon_id: i16,
    pub available: bool,
}

impl DofusSerialize for DungeonKeyRingUpdateMessage {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_var_short(self.dungeon_id);
        writer.write_boolean(self.available);
    }
}

impl DofusDeserialize for DungeonKeyRingUpdateMessage {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            dungeon_id: reader.read_var_short()?,
            available: reader.read_boolean()?,
        })
    }
}

impl DofusMessage for DungeonKeyRingUpdateMessage {
    const MESSAGE_ID: u16 = 6296;
}

/// Protocol message — ID: 6299
#[derive(Debug, Clone, Default)]
pub struct DungeonKeyRingMessage {
    pub availables: Vec<i16>,
    pub unavailables: Vec<i16>,
}

impl DofusSerialize for DungeonKeyRingMessage {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_short(self.availables.len() as _);
        for item in &self.availables {
            writer.write_var_short(*item);
        }
        writer.write_short(self.unavailables.len() as _);
        for item in &self.unavailables {
            writer.write_var_short(*item);
        }
    }
}

impl DofusDeserialize for DungeonKeyRingMessage {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            availables: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(reader.read_var_short()?);
                }
                v
            },
            unavailables: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(reader.read_var_short()?);
                }
                v
            },
        })
    }
}

impl DofusMessage for DungeonKeyRingMessage {
    const MESSAGE_ID: u16 = 6299;
}

