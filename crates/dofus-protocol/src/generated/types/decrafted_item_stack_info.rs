// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusSerialize, DofusType};
use dofus_io::boolean_byte_wrapper;
use super::*;
use anyhow::Result;

/// Protocol type — ID: 3237
#[derive(Debug, Clone, Default)]
pub struct DecraftedItemStackInfo {
    pub object_u_i_d: i32,
    pub bonus_min: f32,
    pub bonus_max: f32,
    pub runes_id: Vec<i16>,
    pub runes_qty: Vec<i32>,
}

impl DofusSerialize for DecraftedItemStackInfo {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_var_int(self.object_u_i_d);
        writer.write_float(self.bonus_min);
        writer.write_float(self.bonus_max);
        writer.write_short(self.runes_id.len() as _);
        for item in &self.runes_id {
            writer.write_var_short(*item);
        }
        writer.write_short(self.runes_qty.len() as _);
        for item in &self.runes_qty {
            writer.write_var_int(*item);
        }
    }
}

impl DofusDeserialize for DecraftedItemStackInfo {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            object_u_i_d: reader.read_var_int()?,
            bonus_min: reader.read_float()?,
            bonus_max: reader.read_float()?,
            runes_id: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(reader.read_var_short()?);
                }
                v
            },
            runes_qty: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(reader.read_var_int()?);
                }
                v
            },
        })
    }
}

impl DofusType for DecraftedItemStackInfo {
    const TYPE_ID: u16 = 3237;
}

