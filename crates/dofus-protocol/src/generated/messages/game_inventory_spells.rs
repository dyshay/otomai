// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusMessage, DofusSerialize};
use dofus_io::boolean_byte_wrapper;
use super::super::types::*;
use anyhow::Result;

/// Protocol message — ID: 3553
#[derive(Debug, Clone, Default)]
pub struct SpellListMessage {
    pub spell_previsualization: bool,
    pub spells: Vec<SpellItem>,
}

impl DofusSerialize for SpellListMessage {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_boolean(self.spell_previsualization);
        writer.write_short(self.spells.len() as _);
        for item in &self.spells {
            item.serialize(writer);
        }
    }
}

impl DofusDeserialize for SpellListMessage {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            spell_previsualization: reader.read_boolean()?,
            spells: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(SpellItem::deserialize(reader)?);
                }
                v
            },
        })
    }
}

impl DofusMessage for SpellListMessage {
    const MESSAGE_ID: u16 = 3553;
}

