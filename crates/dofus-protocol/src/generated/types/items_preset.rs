// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusSerialize, DofusType};
use dofus_io::boolean_byte_wrapper;
use super::*;
use anyhow::Result;

/// Protocol type — ID: 517
#[derive(Debug, Clone, Default)]
pub struct ItemsPreset {
    pub id: i16,
    pub items: Vec<ItemForPreset>,
    pub mount_equipped: bool,
    pub look: EntityLook,
}

impl DofusSerialize for ItemsPreset {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_short(self.id);
        writer.write_short(self.items.len() as _);
        for item in &self.items {
            item.serialize(writer);
        }
        writer.write_boolean(self.mount_equipped);
        self.look.serialize(writer);
    }
}

impl DofusDeserialize for ItemsPreset {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            id: reader.read_short()?,
            items: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(ItemForPreset::deserialize(reader)?);
                }
                v
            },
            mount_equipped: reader.read_boolean()?,
            look: EntityLook::deserialize(reader)?,
        })
    }
}

impl DofusType for ItemsPreset {
    const TYPE_ID: u16 = 517;
}

