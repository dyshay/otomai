// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusSerialize, DofusType};
use dofus_io::boolean_byte_wrapper;
use super::*;
use anyhow::Result;

/// Protocol type — ID: 5533
#[derive(Debug, Clone, Default)]
pub struct BreachReward {
    pub id: i32,
    pub buy_locks: Vec<u8>,
    pub buy_criterion: String,
    pub remaining_qty: i32,
    pub price: i32,
}

impl DofusSerialize for BreachReward {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_var_int(self.id);
        writer.write_short(self.buy_locks.len() as _);
        for item in &self.buy_locks {
            writer.write_byte(*item);
        }
        writer.write_utf(&self.buy_criterion);
        writer.write_var_int(self.remaining_qty);
        writer.write_var_int(self.price);
    }
}

impl DofusDeserialize for BreachReward {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            id: reader.read_var_int()?,
            buy_locks: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(reader.read_byte()?);
                }
                v
            },
            buy_criterion: reader.read_utf()?,
            remaining_qty: reader.read_var_int()?,
            price: reader.read_var_int()?,
        })
    }
}

impl DofusType for BreachReward {
    const TYPE_ID: u16 = 5533;
}

