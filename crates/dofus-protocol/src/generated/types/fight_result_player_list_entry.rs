// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusSerialize, DofusType};
use dofus_io::boolean_byte_wrapper;
use super::*;
use anyhow::Result;

/// Protocol type — ID: 24
#[derive(Debug, Clone, Default)]
pub struct FightResultPlayerListEntry {
    pub outcome: i16,
    pub wave: u8,
    pub rewards: FightLoot,
    pub id: f64,
    pub alive: bool,
    pub level: i16,
    pub additional: Vec<Vec<u8>>,
}

impl DofusSerialize for FightResultPlayerListEntry {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_var_short(self.outcome);
        writer.write_byte(self.wave);
        self.rewards.serialize(writer);
        writer.write_double(self.id);
        writer.write_boolean(self.alive);
        writer.write_var_short(self.level);
        writer.write_short(self.additional.len() as _);
        // polymorphic vector (unresolved base type)
    }
}

impl DofusDeserialize for FightResultPlayerListEntry {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            outcome: reader.read_var_short()?,
            wave: reader.read_byte()?,
            rewards: FightLoot::deserialize(reader)?,
            id: reader.read_double()?,
            alive: reader.read_boolean()?,
            level: reader.read_var_short()?,
            additional: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(Default::default());
                }
                v
            },
        })
    }
}

impl DofusType for FightResultPlayerListEntry {
    const TYPE_ID: u16 = 24;
}

