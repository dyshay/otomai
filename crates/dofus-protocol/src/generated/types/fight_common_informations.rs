// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusSerialize, DofusType};
use dofus_io::boolean_byte_wrapper;
use super::*;
use anyhow::Result;

/// Protocol type — ID: 3668
#[derive(Debug, Clone, Default)]
pub struct FightCommonInformations {
    pub fight_id: i16,
    pub fight_type: u8,
    pub fight_teams: Vec<Vec<u8>>,
    pub fight_teams_positions: Vec<i16>,
    pub fight_teams_options: Vec<FightOptionsInformations>,
}

impl DofusSerialize for FightCommonInformations {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_var_short(self.fight_id);
        writer.write_byte(self.fight_type);
        writer.write_short(self.fight_teams.len() as _);
        // polymorphic vector (unresolved base type)
        writer.write_short(self.fight_teams_positions.len() as _);
        for item in &self.fight_teams_positions {
            writer.write_var_short(*item);
        }
        writer.write_short(self.fight_teams_options.len() as _);
        for item in &self.fight_teams_options {
            item.serialize(writer);
        }
    }
}

impl DofusDeserialize for FightCommonInformations {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            fight_id: reader.read_var_short()?,
            fight_type: reader.read_byte()?,
            fight_teams: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(Default::default());
                }
                v
            },
            fight_teams_positions: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(reader.read_var_short()?);
                }
                v
            },
            fight_teams_options: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(FightOptionsInformations::deserialize(reader)?);
                }
                v
            },
        })
    }
}

impl DofusType for FightCommonInformations {
    const TYPE_ID: u16 = 3668;
}

