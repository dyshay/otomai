// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusSerialize, DofusType};
use dofus_io::boolean_byte_wrapper;
use super::*;
use anyhow::Result;

/// Protocol type — ID: 578
#[derive(Debug, Clone, Default)]
pub struct ExtendedLockedBreachBranch {
    pub room: u8,
    pub element: i32,
    pub bosses: Vec<MonsterInGroupLightInformations>,
    pub map: f64,
    pub score: i16,
    pub relative_score: i16,
    pub monsters: Vec<MonsterInGroupLightInformations>,
    pub rewards: Vec<BreachReward>,
    pub modifier: i32,
    pub prize: i32,
    pub unlock_price: i32,
}

impl DofusSerialize for ExtendedLockedBreachBranch {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_byte(self.room);
        writer.write_int(self.element);
        writer.write_short(self.bosses.len() as _);
        for item in &self.bosses {
            item.serialize(writer);
        }
        writer.write_double(self.map);
        writer.write_short(self.score);
        writer.write_short(self.relative_score);
        writer.write_short(self.monsters.len() as _);
        for item in &self.monsters {
            item.serialize(writer);
        }
        writer.write_short(self.rewards.len() as _);
        for item in &self.rewards {
            item.serialize(writer);
        }
        writer.write_var_int(self.modifier);
        writer.write_var_int(self.prize);
        writer.write_var_int(self.unlock_price);
    }
}

impl DofusDeserialize for ExtendedLockedBreachBranch {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            room: reader.read_byte()?,
            element: reader.read_int()?,
            bosses: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(MonsterInGroupLightInformations::deserialize(reader)?);
                }
                v
            },
            map: reader.read_double()?,
            score: reader.read_short()?,
            relative_score: reader.read_short()?,
            monsters: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(MonsterInGroupLightInformations::deserialize(reader)?);
                }
                v
            },
            rewards: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(BreachReward::deserialize(reader)?);
                }
                v
            },
            modifier: reader.read_var_int()?,
            prize: reader.read_var_int()?,
            unlock_price: reader.read_var_int()?,
        })
    }
}

impl DofusType for ExtendedLockedBreachBranch {
    const TYPE_ID: u16 = 578;
}

