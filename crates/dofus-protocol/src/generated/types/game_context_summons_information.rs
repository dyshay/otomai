// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusSerialize, DofusType};
use dofus_io::boolean_byte_wrapper;
use super::*;
use anyhow::Result;

/// Protocol type — ID: 567
#[derive(Debug, Clone, Default)]
pub struct GameContextSummonsInformation {
    pub spawn_information: Box<SpawnInformationVariant>,
    pub wave: u8,
    pub look: EntityLook,
    pub stats: Box<GameFightMinimalStatsVariant>,
    pub summons: Vec<Vec<u8>>,
}

impl DofusSerialize for GameContextSummonsInformation {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_ushort(self.spawn_information.get_type_id());
        (*self.spawn_information).serialize(writer);
        writer.write_byte(self.wave);
        self.look.serialize(writer);
        writer.write_ushort(self.stats.get_type_id());
        (*self.stats).serialize(writer);
        writer.write_short(self.summons.len() as _);
        // polymorphic vector (unresolved base type)
    }
}

impl DofusDeserialize for GameContextSummonsInformation {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            spawn_information: {
                let type_id = reader.read_ushort()?;
                Box::new(SpawnInformationVariant::deserialize_with_id(type_id, reader)?)
            },
            wave: reader.read_byte()?,
            look: EntityLook::deserialize(reader)?,
            stats: {
                let type_id = reader.read_ushort()?;
                Box::new(GameFightMinimalStatsVariant::deserialize_with_id(type_id, reader)?)
            },
            summons: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(Default::default());
                }
                v
            },
        })
    }
}

impl DofusType for GameContextSummonsInformation {
    const TYPE_ID: u16 = 567;
}

