// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusSerialize, DofusType};
use dofus_io::boolean_byte_wrapper;
use super::*;
use anyhow::Result;

/// Protocol type — ID: 121
#[derive(Debug, Clone, Default)]
pub struct SellerBuyerDescriptor {
    pub quantities: Vec<i32>,
    pub types: Vec<i32>,
    pub tax_percentage: f32,
    pub tax_modification_percentage: f32,
    pub max_item_level: u8,
    pub max_item_per_account: i32,
    pub npc_contextual_id: i32,
    pub unsold_delay: i16,
}

impl DofusSerialize for SellerBuyerDescriptor {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_short(self.quantities.len() as _);
        for item in &self.quantities {
            writer.write_var_int(*item);
        }
        writer.write_short(self.types.len() as _);
        for item in &self.types {
            writer.write_var_int(*item);
        }
        writer.write_float(self.tax_percentage);
        writer.write_float(self.tax_modification_percentage);
        writer.write_byte(self.max_item_level);
        writer.write_var_int(self.max_item_per_account);
        writer.write_int(self.npc_contextual_id);
        writer.write_var_short(self.unsold_delay);
    }
}

impl DofusDeserialize for SellerBuyerDescriptor {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            quantities: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(reader.read_var_int()?);
                }
                v
            },
            types: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(reader.read_var_int()?);
                }
                v
            },
            tax_percentage: reader.read_float()?,
            tax_modification_percentage: reader.read_float()?,
            max_item_level: reader.read_byte()?,
            max_item_per_account: reader.read_var_int()?,
            npc_contextual_id: reader.read_int()?,
            unsold_delay: reader.read_var_short()?,
        })
    }
}

impl DofusType for SellerBuyerDescriptor {
    const TYPE_ID: u16 = 121;
}

