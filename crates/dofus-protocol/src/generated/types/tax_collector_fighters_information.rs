// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusSerialize, DofusType};
use dofus_io::boolean_byte_wrapper;
use super::*;
use anyhow::Result;

/// Protocol type — ID: 169
#[derive(Debug, Clone, Default)]
pub struct TaxCollectorFightersInformation {
    pub collector_id: f64,
    pub ally_characters_informations: Vec<Vec<u8>>,
    pub enemy_characters_informations: Vec<Vec<u8>>,
}

impl DofusSerialize for TaxCollectorFightersInformation {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_double(self.collector_id);
        writer.write_short(self.ally_characters_informations.len() as _);
        // polymorphic vector (unresolved base type)
        writer.write_short(self.enemy_characters_informations.len() as _);
        // polymorphic vector (unresolved base type)
    }
}

impl DofusDeserialize for TaxCollectorFightersInformation {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            collector_id: reader.read_double()?,
            ally_characters_informations: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(Default::default());
                }
                v
            },
            enemy_characters_informations: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(Default::default());
                }
                v
            },
        })
    }
}

impl DofusType for TaxCollectorFightersInformation {
    const TYPE_ID: u16 = 169;
}

