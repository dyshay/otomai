// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusSerialize, DofusType};
use dofus_io::boolean_byte_wrapper;
use super::*;
use anyhow::Result;

/// Protocol type — ID: 33
#[derive(Debug, Clone, Default)]
pub struct FightTeamInformations {
    pub team_id: u8,
    pub leader_id: f64,
    pub team_side: u8,
    pub team_type_id: u8,
    pub nb_waves: u8,
    pub team_members: Vec<Vec<u8>>,
}

impl DofusSerialize for FightTeamInformations {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_byte(self.team_id);
        writer.write_double(self.leader_id);
        writer.write_byte(self.team_side);
        writer.write_byte(self.team_type_id);
        writer.write_byte(self.nb_waves);
        writer.write_short(self.team_members.len() as _);
        // polymorphic vector (unresolved base type)
    }
}

impl DofusDeserialize for FightTeamInformations {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            team_id: reader.read_byte()?,
            leader_id: reader.read_double()?,
            team_side: reader.read_byte()?,
            team_type_id: reader.read_byte()?,
            nb_waves: reader.read_byte()?,
            team_members: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(Default::default());
                }
                v
            },
        })
    }
}

impl DofusType for FightTeamInformations {
    const TYPE_ID: u16 = 33;
}

