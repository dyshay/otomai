// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusSerialize, DofusType};
use dofus_io::boolean_byte_wrapper;
use super::*;
use anyhow::Result;

/// Protocol type — ID: 477
#[derive(Debug, Clone, Default)]
pub struct CharacterToRemodelInformations {
    pub id: i64,
    pub name: String,
    pub breed: u8,
    pub sex: bool,
    pub cosmetic_id: i16,
    pub colors: Vec<i32>,
    pub possible_change_mask: u8,
    pub mandatory_change_mask: u8,
}

impl DofusSerialize for CharacterToRemodelInformations {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_var_long(self.id);
        writer.write_utf(&self.name);
        writer.write_byte(self.breed);
        writer.write_boolean(self.sex);
        writer.write_var_short(self.cosmetic_id);
        writer.write_short(self.colors.len() as _);
        for item in &self.colors {
            writer.write_int(*item);
        }
        writer.write_byte(self.possible_change_mask);
        writer.write_byte(self.mandatory_change_mask);
    }
}

impl DofusDeserialize for CharacterToRemodelInformations {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            id: reader.read_var_long()?,
            name: reader.read_utf()?,
            breed: reader.read_byte()?,
            sex: reader.read_boolean()?,
            cosmetic_id: reader.read_var_short()?,
            colors: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(reader.read_int()?);
                }
                v
            },
            possible_change_mask: reader.read_byte()?,
            mandatory_change_mask: reader.read_byte()?,
        })
    }
}

impl DofusType for CharacterToRemodelInformations {
    const TYPE_ID: u16 = 477;
}

