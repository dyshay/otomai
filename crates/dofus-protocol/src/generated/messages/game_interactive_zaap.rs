// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusMessage, DofusSerialize};
use dofus_io::boolean_byte_wrapper;
use super::super::types::*;
use anyhow::Result;

/// Protocol message — ID: 5961
#[derive(Debug, Clone, Default)]
pub struct TeleportRequestMessage {
    pub source_type: u8,
    pub destination_type: u8,
    pub map_id: f64,
}

impl DofusSerialize for TeleportRequestMessage {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_byte(self.source_type);
        writer.write_byte(self.destination_type);
        writer.write_double(self.map_id);
    }
}

impl DofusDeserialize for TeleportRequestMessage {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            source_type: reader.read_byte()?,
            destination_type: reader.read_byte()?,
            map_id: reader.read_double()?,
        })
    }
}

impl DofusMessage for TeleportRequestMessage {
    const MESSAGE_ID: u16 = 5961;
}

/// Protocol message — ID: 6571
#[derive(Debug, Clone, Default)]
pub struct ZaapRespawnUpdatedMessage {
    pub map_id: f64,
}

impl DofusSerialize for ZaapRespawnUpdatedMessage {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_double(self.map_id);
    }
}

impl DofusDeserialize for ZaapRespawnUpdatedMessage {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            map_id: reader.read_double()?,
        })
    }
}

impl DofusMessage for ZaapRespawnUpdatedMessage {
    const MESSAGE_ID: u16 = 6571;
}

/// Protocol message — ID: 6572
#[derive(Debug, Clone, Default)]
pub struct ZaapRespawnSaveRequestMessage {
}

impl DofusSerialize for ZaapRespawnSaveRequestMessage {
    fn serialize(&self, writer: &mut BigEndianWriter) {
    }
}

impl DofusDeserialize for ZaapRespawnSaveRequestMessage {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
        })
    }
}

impl DofusMessage for ZaapRespawnSaveRequestMessage {
    const MESSAGE_ID: u16 = 6572;
}

/// Protocol message — ID: 6829
#[derive(Debug, Clone, Default)]
pub struct TeleportDestinationsMessage {
    pub r#type: u8,
    pub destinations: Vec<TeleportDestination>,
}

impl DofusSerialize for TeleportDestinationsMessage {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_byte(self.r#type);
        writer.write_short(self.destinations.len() as _);
        for item in &self.destinations {
            item.serialize(writer);
        }
    }
}

impl DofusDeserialize for TeleportDestinationsMessage {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            r#type: reader.read_byte()?,
            destinations: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(TeleportDestination::deserialize(reader)?);
                }
                v
            },
        })
    }
}

impl DofusMessage for TeleportDestinationsMessage {
    const MESSAGE_ID: u16 = 6829;
}

/// Protocol message — ID: 6830
#[derive(Debug, Clone, Default)]
pub struct ZaapDestinationsMessage {
    pub r#type: u8,
    pub destinations: Vec<TeleportDestination>,
    pub spawn_map_id: f64,
}

impl DofusSerialize for ZaapDestinationsMessage {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_byte(self.r#type);
        writer.write_short(self.destinations.len() as _);
        for item in &self.destinations {
            item.serialize(writer);
        }
        writer.write_double(self.spawn_map_id);
    }
}

impl DofusDeserialize for ZaapDestinationsMessage {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            r#type: reader.read_byte()?,
            destinations: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(TeleportDestination::deserialize(reader)?);
                }
                v
            },
            spawn_map_id: reader.read_double()?,
        })
    }
}

impl DofusMessage for ZaapDestinationsMessage {
    const MESSAGE_ID: u16 = 6830;
}

