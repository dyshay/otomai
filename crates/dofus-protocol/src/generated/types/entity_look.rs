// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusSerialize, DofusType};
use dofus_io::boolean_byte_wrapper;
use super::*;
use anyhow::Result;

/// Protocol type — ID: 55
#[derive(Debug, Clone, Default)]
pub struct EntityLook {
    pub bones_id: i16,
    pub skins: Vec<i16>,
    pub indexed_colors: Vec<i32>,
    pub scales: Vec<i16>,
    pub subentities: Vec<SubEntity>,
}

impl DofusSerialize for EntityLook {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_var_short(self.bones_id);
        writer.write_short(self.skins.len() as _);
        for item in &self.skins {
            writer.write_var_short(*item);
        }
        writer.write_short(self.indexed_colors.len() as _);
        for item in &self.indexed_colors {
            writer.write_int(*item);
        }
        writer.write_short(self.scales.len() as _);
        for item in &self.scales {
            writer.write_var_short(*item);
        }
        writer.write_short(self.subentities.len() as _);
        for item in &self.subentities {
            item.serialize(writer);
        }
    }
}

impl DofusDeserialize for EntityLook {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            bones_id: reader.read_var_short()?,
            skins: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(reader.read_var_short()?);
                }
                v
            },
            indexed_colors: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(reader.read_int()?);
                }
                v
            },
            scales: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(reader.read_var_short()?);
                }
                v
            },
            subentities: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(SubEntity::deserialize(reader)?);
                }
                v
            },
        })
    }
}

impl DofusType for EntityLook {
    const TYPE_ID: u16 = 55;
}

