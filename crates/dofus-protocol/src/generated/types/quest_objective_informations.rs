// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusSerialize, DofusType};
use dofus_io::boolean_byte_wrapper;
use super::*;
use anyhow::Result;

/// Protocol type — ID: 8695
#[derive(Debug, Clone, Default)]
pub struct QuestObjectiveInformations {
    pub objective_id: i16,
    pub objective_status: bool,
    pub dialog_params: Vec<String>,
}

impl DofusSerialize for QuestObjectiveInformations {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_var_short(self.objective_id);
        writer.write_boolean(self.objective_status);
        writer.write_short(self.dialog_params.len() as _);
        for item in &self.dialog_params {
            writer.write_utf(item);
        }
    }
}

impl DofusDeserialize for QuestObjectiveInformations {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            objective_id: reader.read_var_short()?,
            objective_status: reader.read_boolean()?,
            dialog_params: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(reader.read_utf()?);
                }
                v
            },
        })
    }
}

impl DofusType for QuestObjectiveInformations {
    const TYPE_ID: u16 = 8695;
}

