// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusSerialize, DofusType};
use dofus_io::boolean_byte_wrapper;
use super::*;
use anyhow::Result;

/// Protocol type — ID: 4830
#[derive(Debug, Clone, Default)]
pub struct Achievement {
    pub id: i16,
    pub finished_objective: Vec<AchievementObjective>,
    pub started_objectives: Vec<AchievementStartedObjective>,
}

impl DofusSerialize for Achievement {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_var_short(self.id);
        writer.write_short(self.finished_objective.len() as _);
        for item in &self.finished_objective {
            item.serialize(writer);
        }
        writer.write_short(self.started_objectives.len() as _);
        for item in &self.started_objectives {
            item.serialize(writer);
        }
    }
}

impl DofusDeserialize for Achievement {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            id: reader.read_var_short()?,
            finished_objective: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(AchievementObjective::deserialize(reader)?);
                }
                v
            },
            started_objectives: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(AchievementStartedObjective::deserialize(reader)?);
                }
                v
            },
        })
    }
}

impl DofusType for Achievement {
    const TYPE_ID: u16 = 4830;
}

