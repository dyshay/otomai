// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusSerialize, DofusType};
use dofus_io::boolean_byte_wrapper;
use super::*;
use anyhow::Result;

/// Protocol type — ID: 9023
#[derive(Debug, Clone, Default)]
pub struct GameRolePlayNpcQuestFlag {
    pub quests_to_valid_id: Vec<i16>,
    pub quests_to_start_id: Vec<i16>,
}

impl DofusSerialize for GameRolePlayNpcQuestFlag {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_short(self.quests_to_valid_id.len() as _);
        for item in &self.quests_to_valid_id {
            writer.write_var_short(*item);
        }
        writer.write_short(self.quests_to_start_id.len() as _);
        for item in &self.quests_to_start_id {
            writer.write_var_short(*item);
        }
    }
}

impl DofusDeserialize for GameRolePlayNpcQuestFlag {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            quests_to_valid_id: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(reader.read_var_short()?);
                }
                v
            },
            quests_to_start_id: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(reader.read_var_short()?);
                }
                v
            },
        })
    }
}

impl DofusType for GameRolePlayNpcQuestFlag {
    const TYPE_ID: u16 = 9023;
}

