// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusSerialize, DofusType};
use dofus_io::boolean_byte_wrapper;
use super::*;
use anyhow::Result;

/// Protocol type — ID: 63
#[derive(Debug, Clone, Default)]
pub struct EntityMovementInformations {
    pub id: i32,
    pub steps: Vec<u8>,
}

impl DofusSerialize for EntityMovementInformations {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_int(self.id);
        writer.write_short(self.steps.len() as _);
        for item in &self.steps {
            writer.write_byte(*item);
        }
    }
}

impl DofusDeserialize for EntityMovementInformations {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            id: reader.read_int()?,
            steps: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(reader.read_byte()?);
                }
                v
            },
        })
    }
}

impl DofusType for EntityMovementInformations {
    const TYPE_ID: u16 = 63;
}

