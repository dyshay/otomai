// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusSerialize, DofusType};
use dofus_io::boolean_byte_wrapper;
use super::*;
use anyhow::Result;

/// Protocol type — ID: 510
#[derive(Debug, Clone, Default)]
pub struct HouseOnMapInformations {
    pub house_id: i32,
    pub model_id: i16,
    pub doors_on_map: Vec<i32>,
    pub house_instances: Vec<HouseInstanceInformations>,
}

impl DofusSerialize for HouseOnMapInformations {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_var_int(self.house_id);
        writer.write_var_short(self.model_id);
        writer.write_short(self.doors_on_map.len() as _);
        for item in &self.doors_on_map {
            writer.write_int(*item);
        }
        writer.write_short(self.house_instances.len() as _);
        for item in &self.house_instances {
            item.serialize(writer);
        }
    }
}

impl DofusDeserialize for HouseOnMapInformations {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            house_id: reader.read_var_int()?,
            model_id: reader.read_var_short()?,
            doors_on_map: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(reader.read_int()?);
                }
                v
            },
            house_instances: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(HouseInstanceInformations::deserialize(reader)?);
                }
                v
            },
        })
    }
}

impl DofusType for HouseOnMapInformations {
    const TYPE_ID: u16 = 510;
}

