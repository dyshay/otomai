// Auto-generated by protocol-gen — do not edit
#![allow(unused_imports, dead_code)]

use dofus_io::{BigEndianReader, BigEndianWriter, DofusDeserialize, DofusSerialize, DofusType};
use dofus_io::boolean_byte_wrapper;
use super::*;
use anyhow::Result;

/// Protocol type — ID: 585
#[derive(Debug, Clone, Default)]
pub struct IconNamedPreset {
    pub id: i16,
    pub presets: Vec<Vec<u8>>,
    pub icon_id: i16,
    pub name: String,
}

impl DofusSerialize for IconNamedPreset {
    fn serialize(&self, writer: &mut BigEndianWriter) {
        writer.write_short(self.id);
        writer.write_short(self.presets.len() as _);
        // polymorphic vector (unresolved base type)
        writer.write_short(self.icon_id);
        writer.write_utf(&self.name);
    }
}

impl DofusDeserialize for IconNamedPreset {
    fn deserialize(reader: &mut BigEndianReader) -> Result<Self> {
        Ok(Self {
            id: reader.read_short()?,
            presets: {
                let count = reader.read_ushort()? as usize;
                let mut v = Vec::with_capacity(count);
                for _ in 0..count {
                    v.push(Default::default());
                }
                v
            },
            icon_id: reader.read_short()?,
            name: reader.read_utf()?,
        })
    }
}

impl DofusType for IconNamedPreset {
    const TYPE_ID: u16 = 585;
}

